# Settings in the [build] context are global and are applied to all contexts
# unless otherwise overridden by more specific contexts.
[build]
  # Directory to change to before starting a build.
  # This is where we will look for package.json/.nvmrc/etc.
  base = "/"


# The following redirect is intended for use with most SPAs that handle
# routing internally.
[[redirects]]
  from = "/*"
  to = "index.html"
  status = 200

# Specific branch context: all deploys from this specific branch will inherit
# these settings.
# For contexts of branches with special characters, enclose the branch name
# with quotes.
[context."release/online-offers-test"] # 'release/online-offers-test' is a branch name

  # Build command.
  command = "yarn clean && lerna exec --scope @loyaltycorp/manage-online-offers -- yarn build"

  # Directory (relative to root of your repo) that contains the deploy-ready
  # HTML files and assets generated by the build. If a base directory has
  # been specified, include it in the publish directory path.
  publish = "./packages/online-offers/dist"

  environment = { VUE_APP_API_URL = "https://api.dev.oo.eonx.com", VUE_APP_AUTH0_AUDIENCE="https://api.dev.online-offers.com.au"}


[context."release/payments-test"]
  command = "yarn clean && lerna exec --scope @loyaltycorp/manage-payments -- yarn build"
  publish = "./packages/payments/dist"
  environment = { VUE_APP_API_URL = "https://api.dev.payments.eonx.com/", VUE_APP_AUTH0_AUDIENCE="https://api.dev.payments.eonx.com/", VUE_APP_ELASTIC_HOST="https://api.dev.payments.eonx.com/search" }
